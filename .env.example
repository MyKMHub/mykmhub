# Server-only OpenAI credential. Never expose this with a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=

# Keep false for public deployments until authentication, rate limiting, and
# spending controls are in place.
IMAGE_GENERATION_ENABLED=false
