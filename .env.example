# Optional server-side provider credentials. Never expose these with a
# NEXT_PUBLIC_ prefix. Users may instead enter the selected provider's key in
# the tool for one request.
OPENAI_API_KEY=
GEMINI_API_KEY=
BFL_API_KEY=
